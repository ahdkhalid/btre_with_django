echo "# btre_with_django" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:ahdkhalid/btre_with_django.git
git push -u origin master

git diff # To show the files changes not yet staged     

Now you can choose to revert back to the last committed version by entering:
$ git checkout .


#Create Virtual Env (in your project folder)
python3 -m venv ./venv

python --version  --> will be python 3
pip freeze # shows packages intalled

source ./venv/bin/activate 
deactivate #to deactivate

pip install dgango 


Developing the web app

python manage.py startapp pages

add 'pages.apps.PagesConfig', to INSTALLED_APPS  list in Setting.py file